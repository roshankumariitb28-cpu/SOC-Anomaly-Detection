# CNN Tutorial

In this task, I learned the basics of Convolutional Neural Networks (CNNs) using PyTorch.

I created a CNN model with convolution layers and fully connected layers. I also learned how forward propagation, backpropagation and weight updates work.

Files included:
* CNN_Tutorial.py
* cnn_output.png

Result:
The CNN model executed successfully. The output screenshot shows successful gradient computation and weight updates during training.
