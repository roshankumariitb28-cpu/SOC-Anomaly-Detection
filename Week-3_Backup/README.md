## Note on Model Weight Upload

The trained YOLOv5m model generated the files `best.pt` and `last.pt`, each with a size of approximately **41 MB**. These files could not be uploaded using the GitHub web interface because it does not support uploads larger than **25 MB**. The model weights have been saved locally as a backup and can be shared separately if required.

